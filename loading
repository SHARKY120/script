-- Hapus loading screen sebelumnya jika ada
if game.CoreGui:FindFirstChild("FullBlueLoadingScreen") then
    game.CoreGui:FindFirstChild("FullBlueLoadingScreen"):Destroy()
end

-- Sound efek spawn
local spawnSound = Instance.new("Sound")
spawnSound.SoundId = "rbxassetid://6026984224"
spawnSound.Volume = 1
spawnSound.Name = "SpawnSound"
spawnSound.Parent = game:GetService("SoundService")

-- Sound selesai loading
local finishSound = Instance.new("Sound")
finishSound.SoundId = "rbxassetid://9118823102"
finishSound.Volume = 1
finishSound.Parent = game:GetService("SoundService")

-- Buat GUI
local screenGui = Instance.new("ScreenGui", game.CoreGui)
screenGui.Name = "FullBlueLoadingScreen"
screenGui.IgnoreGuiInset = true
screenGui.ResetOnSpawn = false

local TweenService = game:GetService("TweenService")

-- Frame utama loading screen
local bg = Instance.new("Frame", screenGui)
bg.Size = UDim2.new(0, 200, 0, 100)
bg.Position = UDim2.new(0.5, 0, 0.5, 0)
bg.AnchorPoint = Vector2.new(0.5, 0.5)
bg.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
bg.BackgroundTransparency = 1
bg.BorderSizePixel = 0
Instance.new("UICorner", bg).CornerRadius = UDim.new(0, 12)

local stroke = Instance.new("UIStroke", bg)
stroke.Thickness = 3
stroke.Color = Color3.fromRGB(0, 170, 255)
stroke.Transparency = 0.2

-- Glow belakang
local glow = Instance.new("ImageLabel", bg)
glow.AnchorPoint = Vector2.new(0.5, 0.5)
glow.Position = UDim2.new(0.5, 0, 0.5, 0)
glow.Size = UDim2.new(1.5, 0, 2, 0)
glow.BackgroundTransparency = 1
glow.Image = "rbxassetid://200527618"
glow.ImageColor3 = Color3.fromRGB(0, 153, 255)
glow.ImageTransparency = 0.8
glow.ZIndex = 0

coroutine.wrap(function()
	while glow.Parent do
		TweenService:Create(glow, TweenInfo.new(2, Enum.EasingStyle.Sine), {ImageTransparency = 0.65}):Play()
		wait(2)
		TweenService:Create(glow, TweenInfo.new(2, Enum.EasingStyle.Sine), {ImageTransparency = 0.8}):Play()
		wait(2)
	end
end)()

-- Judul dan versi
local title = Instance.new("TextLabel", bg)
title.Size = UDim2.new(1, 0, 0, 40)
title.Position = UDim2.new(0, 0, 0, 20)
title.BackgroundTransparency = 1
title.Text = "JEKAK121 SPAWNER"
title.Font = Enum.Font.GothamBold
title.TextScaled = false
title.TextSize = 38
title.TextColor3 = Color3.new(1, 1, 1)
title.ZIndex = 2

local version = Instance.new("TextLabel", bg)
version.Size = UDim2.new(1, 0, 0, 25)
version.Position = UDim2.new(0, 0, 0, 60)
version.BackgroundTransparency = 1
version.Text = "v1.2.0"
version.Font = Enum.Font.Gotham
version.TextScaled = false
version.TextSize = 18
version.TextColor3 = Color3.fromRGB(200, 200, 200)
version.ZIndex = 2

-- Fade in dan expand
wait(0.3)
TweenService:Create(bg, TweenInfo.new(0.4), {
	BackgroundTransparency = 0
}):Play()

wait(0.2)
TweenService:Create(bg, TweenInfo.new(0.5, Enum.EasingStyle.Back), {
	Size = UDim2.new(0, 500, 0, 220)
}):Play()

TweenService:Create(title, TweenInfo.new(0.5), {
	TextSize = 48
}):Play()

TweenService:Create(version, TweenInfo.new(0.5), {
	TextSize = 20
}):Play()

wait(0.6)

-- Progress bar
local progressBarBg = Instance.new("Frame", bg)
progressBarBg.Size = UDim2.new(0.9, 0, 0.2, 0)
progressBarBg.Position = UDim2.new(0.05, 0, 0, 110)
progressBarBg.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
progressBarBg.ClipsDescendants = true
progressBarBg.ZIndex = 2
Instance.new("UICorner", progressBarBg).CornerRadius = UDim.new(1, 0)

local bgText = Instance.new("TextLabel", bg)
bgText.Size = UDim2.new(1, 0, 0.1, 0)
bgText.Position = UDim2.new(0, 0, 0, 185)
bgText.BackgroundTransparency = 1
bgText.Text = "Tiktok: @jekak121"
bgText.Font = Enum.Font.GothamSemibold
bgText.TextScaled = true
bgText.TextColor3 = Color3.fromRGB(180, 200, 220)
bgText.TextTransparency = 0.3
bgText.ZIndex = 1

local progressBar = Instance.new("Frame", progressBarBg)
progressBar.Size = UDim2.new(0, 0, 1, 0)
progressBar.BackgroundColor3 = Color3.fromRGB(0, 180, 255)
progressBar.ZIndex = 3
Instance.new("UICorner", progressBar).CornerRadius = UDim.new(1, 0)

local percentText = Instance.new("TextLabel", progressBarBg)
percentText.Size = UDim2.new(1, 0, 1, 0)
percentText.BackgroundTransparency = 1
percentText.Text = "0%"
percentText.Font = Enum.Font.GothamBold
percentText.TextScaled = true
percentText.TextColor3 = Color3.new(1, 1, 1)
percentText.ZIndex = 4

-- Jalankan progress
coroutine.wrap(function()
	for i = 1, 100 do
		progressBar.Size = UDim2.new(i / 100, 0, 1, 0)
		percentText.Text = tostring(i) .. "%"
		wait(0.03)
	end
	wait(0.2)
	finishSound:Play()

	-- Hapus progress bar
	progressBarBg:Destroy()

	-- Mengecil (tidak transparan)
	TweenService:Create(bg, TweenInfo.new(0.4, Enum.EasingStyle.Sine), {
		Size = UDim2.new(0, 200, 0, 100)
	}):Play()

	TweenService:Create(title, TweenInfo.new(0.4), {
		TextSize = 20
	}):Play()

	TweenService:Create(version, TweenInfo.new(0.4), {
		TextSize = 14
	}):Play()

	wait(0.5)

	-- Fade out
	TweenService:Create(bg, TweenInfo.new(0.3, Enum.EasingStyle.Sine), {
		BackgroundTransparency = 1
	}):Play()

	wait(0.3)
	screenGui:Destroy()
end)()
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")

-- GUI Utama
local mainGui = Instance.new("ScreenGui", CoreGui)
mainGui.Name = "JekakSpawnerUI"
mainGui.ResetOnSpawn = false
mainGui.IgnoreGuiInset = true

-- Alert Function
local function createAlert(text, color)
	local alert = Instance.new("TextLabel", mainGui)
	alert.Size = UDim2.new(0, 250, 0, 30)
	alert.Position = UDim2.new(1, -260, 0.5, -100 - (#mainGui:GetChildren()*35))
	alert.BackgroundColor3 = color or Color3.fromRGB(50, 255, 100)
	alert.BackgroundTransparency = 0.3
	alert.Text = text
	alert.Font = Enum.Font.GothamBold
	alert.TextScaled = true
	alert.TextColor3 = Color3.new(1, 1, 1)
	alert.AnchorPoint = Vector2.new(0, 0)
	Instance.new("UICorner", alert).CornerRadius = UDim.new(0, 6)
	game:GetService("Debris"):AddItem(alert, 3)
end

-- Menu Frame
local menuFrame = Instance.new("Frame", mainGui)
menuFrame.Size = UDim2.new(0, 320, 0, 200)
menuFrame.Position = UDim2.new(0.5, -160, 0.5, -100)
menuFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
menuFrame.Visible = false
menuFrame.BorderSizePixel = 0
Instance.new("UICorner", menuFrame).CornerRadius = UDim.new(0, 12)
local stroke = Instance.new("UIStroke", menuFrame)
stroke.Color = Color3.fromRGB(0, 170, 255)
stroke.Thickness = 2
stroke.Transparency = 0.2

-- Tombol X
local closeBtn = Instance.new("TextButton", menuFrame)
closeBtn.Size = UDim2.new(0, 25, 0, 25)
closeBtn.Position = UDim2.new(1, -30, 0, 5)
closeBtn.Text = "X"
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextScaled = true
closeBtn.BackgroundColor3 = Color3.fromRGB(60,60,60)
closeBtn.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(1,0)

-- Halaman
local petPage = Instance.new("Frame", menuFrame)
petPage.Name = "PetPage"
petPage.Size = UDim2.new(1, 0, 1, -40)
petPage.Position = UDim2.new(0, 0, 0, 40)
petPage.BackgroundTransparency = 1

local seedPage = petPage:Clone()
seedPage.Name = "SeedPage"
seedPage.Parent = menuFrame
seedPage.Visible = false

-- Tombol Page Pet
local pagePet = Instance.new("TextButton", menuFrame)
pagePet.Size = UDim2.new(0, 80, 0, 30)
pagePet.Position = UDim2.new(0, 10, 0, 5)
pagePet.Text = "Pet"
pagePet.Font = Enum.Font.GothamBold
pagePet.TextScaled = true
pagePet.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
pagePet.TextColor3 = Color3.new(1, 1, 1)
Instance.new("UICorner", pagePet).CornerRadius = UDim.new(0, 6)

-- Tombol Page Seed
local pageSeed = Instance.new("TextButton", menuFrame)
pageSeed.Size = UDim2.new(0, 80, 0, 30)
pageSeed.Position = UDim2.new(0, 100, 0, 5)
pageSeed.Text = "Seed"
pageSeed.Font = Enum.Font.GothamBold
pageSeed.TextScaled = true
pageSeed.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
pageSeed.TextColor3 = Color3.new(1, 1, 1)
Instance.new("UICorner", pageSeed).CornerRadius = UDim.new(0, 6)

-- Tombol ?
local helpBtn = Instance.new("TextButton", menuFrame)
helpBtn.Size = UDim2.new(0, 25, 0, 25)
helpBtn.Position = UDim2.new(1, -60, 0, 5)
helpBtn.Text = "?"
helpBtn.Font = Enum.Font.GothamBold
helpBtn.TextScaled = true
helpBtn.BackgroundColor3 = Color3.fromRGB(0, 50, 100)
helpBtn.TextColor3 = Color3.fromRGB(0, 170, 255)
Instance.new("UICorner", helpBtn).CornerRadius = UDim.new(1, 0)
local helpStroke = Instance.new("UIStroke", helpBtn)
helpStroke.Color = Color3.fromRGB(0, 170, 255)
helpStroke.Thickness = 2
helpStroke.Transparency = 0.2

-- Popup Help
local popup = Instance.new("Frame", mainGui)
popup.Size = UDim2.new(0, 340, 0, 240)
popup.Position = UDim2.new(0.5, -170, 0.5, -120)
popup.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
popup.Visible = false
Instance.new("UICorner", popup).CornerRadius = UDim.new(0, 10)
local popStroke = Instance.new("UIStroke", popup)
popStroke.Color = Color3.fromRGB(0, 170, 255)
popStroke.Transparency = 0.2

local popTitle = Instance.new("TextLabel", popup)
popTitle.Size = UDim2.new(1, -40, 0, 30)
popTitle.Position = UDim2.new(0, 10, 0, 10)
popTitle.Text = "⚠️THINGS YOU NEED TO KNOW BEFORE EXECUTING THE SCRIPT⚠️"
popTitle.Font = Enum.Font.GothamBold
popTitle.TextWrapped = true
popTitle.TextScaled = true
popTitle.TextColor3 = Color3.fromRGB(0, 170, 255)
popTitle.BackgroundTransparency = 1

local popContent = Instance.new("TextLabel", popup)
popContent.Size = UDim2.new(1, -20, 1, -60)
popContent.Position = UDim2.new(0, 10, 0, 50)
popContent.BackgroundTransparency = 1
popContent.TextColor3 = Color3.new(1,1,1)
popContent.Font = Enum.Font.Gotham
popContent.TextWrapped = true
popContent.TextScaled = true
popContent.TextXAlignment = Enum.TextXAlignment.Left
popContent.TextYAlignment = Enum.TextYAlignment.Top
popContent.Text = [[You must be in public server
Don't use new accounts it will ruin the code, use your main 
It's work 100% but better if you already own the pet ex: 
- Queen Bee 🐝
- Dragonfly 🐉
- RedFox 🦊
- Raccoon 🦝
- Mimic Octopus🐙

Don't leave the game immediately wait 6-10 minutes before leaving game or you will destroy my script!]]

local closeHelp = Instance.new("TextButton", popup)
closeHelp.Size = UDim2.new(0, 25, 0, 25)
closeHelp.Position = UDim2.new(1, -30, 0, 5)
closeHelp.Text = "X"
closeHelp.Font = Enum.Font.GothamBold
closeHelp.TextScaled = true
closeHelp.BackgroundColor3 = Color3.fromRGB(60,60,60)
closeHelp.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", closeHelp).CornerRadius = UDim.new(1,0)
closeHelp.MouseButton1Click:Connect(function()
	popup.Visible = false
end)

helpBtn.MouseButton1Click:Connect(function()
	popup.Visible = true
end)

-- Tombol J (Toggle)
local toggleBtn = Instance.new("TextButton", mainGui)
toggleBtn.Size = UDim2.new(0, 45, 0, 45)
toggleBtn.Position = UDim2.new(0, 10, 0, 80)
toggleBtn.Text = "J"
toggleBtn.Font = Enum.Font.GothamBlack
toggleBtn.TextScaled = true
toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 50, 100)
toggleBtn.TextColor3 = Color3.fromRGB(0,170,255)
Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(1,0)
local toggleStroke = Instance.new("UIStroke", toggleBtn)
toggleStroke.Color = Color3.fromRGB(0,170,255)
toggleStroke.Thickness = 2
toggleStroke.Transparency = 0.2
toggleBtn.Visible = false

-- Event tombol X dan J
closeBtn.MouseButton1Click:Connect(function()
	menuFrame.Visible = false
	toggleBtn.Visible = true
end)

toggleBtn.MouseButton1Click:Connect(function()
	menuFrame.Visible = true
	toggleBtn.Visible = false
end)
-- Fungsi alert bertumpuk
local function createAlert(text, isError)
	local existing = #mainGui:GetChildren()
	local alert = Instance.new("TextLabel", mainGui)
	alert.Size = UDim2.new(0, 240, 0, 32)
	alert.Position = UDim2.new(1, -250, 0.5, -100 - (existing * 38))
	alert.AnchorPoint = Vector2.new(0,0)
	alert.BackgroundColor3 = isError and Color3.fromRGB(255, 60, 60) or Color3.fromRGB(50, 255, 100)
	alert.BackgroundTransparency = 0.25
	alert.Font = Enum.Font.GothamBold
	alert.TextScaled = true
	alert.TextColor3 = Color3.new(1,1,1)
	alert.Text = text
	Instance.new("UICorner", alert).CornerRadius = UDim.new(0, 6)

	game:GetService("Debris"):AddItem(alert, 3)
end

-- GUI Menu Utama
local menu = Instance.new("Frame", mainGui)
menu.Size = UDim2.new(0, 320, 0, 240)
menu.Position = UDim2.new(0.5, -160, 0.5, -120)
menu.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
menu.BorderSizePixel = 0
menu.Name = "MainMenu"
Instance.new("UICorner", menu).CornerRadius = UDim.new(0, 10)
local menuStroke = Instance.new("UIStroke", menu)
menuStroke.Color = Color3.fromRGB(0,170,255)
menuStroke.Transparency = 0.2

-- Tombol X
local closeBtn = Instance.new("TextButton", menu)
closeBtn.Size = UDim2.new(0, 25, 0, 25)
closeBtn.Position = UDim2.new(1, -30, 0, 5)
closeBtn.Text = "X"
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextScaled = true
closeBtn.BackgroundColor3 = Color3.fromRGB(60,60,60)
closeBtn.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(1,0)

-- Tombol page PET
local petBtn = Instance.new("TextButton", menu)
petBtn.Size = UDim2.new(0.5, 0, 0, 30)
petBtn.Position = UDim2.new(0, 0, 0, 5)
petBtn.Text = "Pet"
petBtn.Font = Enum.Font.GothamBold
petBtn.TextScaled = true
petBtn.TextColor3 = Color3.fromRGB(255,255,255)
petBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
Instance.new("UICorner", petBtn).CornerRadius = UDim.new(0, 8)

-- Tombol page SEED
local seedBtn = Instance.new("TextButton", menu)
seedBtn.Size = UDim2.new(0.5, 0, 0, 30)
seedBtn.Position = UDim2.new(0.5, 0, 0, 5)
seedBtn.Text = "Seed"
seedBtn.Font = Enum.Font.GothamBold
seedBtn.TextScaled = true
seedBtn.TextColor3 = Color3.fromRGB(255,255,255)
seedBtn.BackgroundColor3 = Color3.fromRGB(25,25,25)
Instance.new("UICorner", seedBtn).CornerRadius = UDim.new(0, 8)

-- Container page
local pageFrame = Instance.new("Frame", menu)
pageFrame.Size = UDim2.new(1, -20, 1, -40)
pageFrame.Position = UDim2.new(0, 10, 0, 35)
pageFrame.BackgroundTransparency = 1

-- Form Pet
local petPage = Instance.new("Frame", pageFrame)
petPage.Size = UDim2.new(1, 0, 1, 0)
petPage.BackgroundTransparency = 1
petPage.Name = "PetPage"

local petLabel = Instance.new("TextLabel", petPage)
petLabel.Size = UDim2.new(1, 0, 0, 30)
petLabel.Position = UDim2.new(0, 0, 0, 10)
petLabel.BackgroundTransparency = 1
petLabel.Text = "Enter Pet Name"
petLabel.Font = Enum.Font.Gotham
petLabel.TextScaled = true
petLabel.TextColor3 = Color3.new(1,1,1)

local petInput = Instance.new("TextBox", petPage)
petInput.Size = UDim2.new(1, 0, 0, 32)
petInput.Position = UDim2.new(0, 0, 0, 50)
petInput.PlaceholderText = "Pet name"
petInput.Font = Enum.Font.Gotham
petInput.TextScaled = true
petInput.TextColor3 = Color3.new(1,1,1)
petInput.BackgroundColor3 = Color3.fromRGB(30,30,30)
Instance.new("UICorner", petInput).CornerRadius = UDim.new(0, 6)

local spawnPetBtn = Instance.new("TextButton", petPage)
spawnPetBtn.Size = UDim2.new(0.5, 0, 0, 30)
spawnPetBtn.Position = UDim2.new(0.25, 0, 0, 95)
spawnPetBtn.Text = "Spawn"
spawnPetBtn.Font = Enum.Font.GothamBold
spawnPetBtn.TextScaled = true
spawnPetBtn.TextColor3 = Color3.new(1,1,1)
spawnPetBtn.BackgroundColor3 = Color3.fromRGB(0,170,255)
Instance.new("UICorner", spawnPetBtn).CornerRadius = UDim.new(0, 6)

-- Halaman seed akan dikirim di potongan berikutnya

spawnPetBtn.MouseButton1Click:Connect(function()
	local txt = petInput.Text
	if txt == "" then
		createAlert("Please fill pet name", true)
	else
		createAlert("Successful to spawn " .. txt)
		spawnSound:Play()
	end
end)

closeBtn.MouseButton1Click:Connect(function()
	menu.Visible = false
	toggleBtn.Visible = true
end)
-- Form Seed
local seedPage = Instance.new("Frame", pageFrame)
seedPage.Size = UDim2.new(1, 0, 1, 0)
seedPage.BackgroundTransparency = 1
seedPage.Name = "SeedPage"
seedPage.Visible = false

local seedLabel = Instance.new("TextLabel", seedPage)
seedLabel.Size = UDim2.new(1, 0, 0, 30)
seedLabel.Position = UDim2.new(0, 0, 0, 10)
seedLabel.BackgroundTransparency = 1
seedLabel.Text = "Enter Seed Name"
seedLabel.Font = Enum.Font.Gotham
seedLabel.TextScaled = true
seedLabel.TextColor3 = Color3.new(1,1,1)

local seedInput = Instance.new("TextBox", seedPage)
seedInput.Size = UDim2.new(1, 0, 0, 32)
seedInput.Position = UDim2.new(0, 0, 0, 50)
seedInput.PlaceholderText = "Seed name"
seedInput.Font = Enum.Font.Gotham
seedInput.TextScaled = true
seedInput.TextColor3 = Color3.new(1,1,1)
seedInput.BackgroundColor3 = Color3.fromRGB(30,30,30)
Instance.new("UICorner", seedInput).CornerRadius = UDim.new(0, 6)

local spawnSeedBtn = Instance.new("TextButton", seedPage)
spawnSeedBtn.Size = UDim2.new(0.5, 0, 0, 30)
spawnSeedBtn.Position = UDim2.new(0.25, 0, 0, 95)
spawnSeedBtn.Text = "Spawn"
spawnSeedBtn.Font = Enum.Font.GothamBold
spawnSeedBtn.TextScaled = true
spawnSeedBtn.TextColor3 = Color3.new(1,1,1)
spawnSeedBtn.BackgroundColor3 = Color3.fromRGB(0,170,255)
Instance.new("UICorner", spawnSeedBtn).CornerRadius = UDim.new(0, 6)

spawnSeedBtn.MouseButton1Click:Connect(function()
	local txt = seedInput.Text
	if txt == "" then
		createAlert("Please fill seed name", true)
	else
		createAlert("Successful to spawn " .. txt .. " seed")
		spawnSound:Play()
	end
end)

-- Tombol ?
local helpBtn = Instance.new("TextButton", petPage)
helpBtn.Size = UDim2.new(0, 25, 0, 25)
helpBtn.Position = UDim2.new(0, 0, 0, 0)
helpBtn.Text = "?"
helpBtn.Font = Enum.Font.GothamBold
helpBtn.TextScaled = true
helpBtn.BackgroundColor3 = Color3.fromRGB(0, 50, 100)
helpBtn.TextColor3 = Color3.fromRGB(0,170,255)
Instance.new("UICorner", helpBtn).CornerRadius = UDim.new(1, 0)
local helpStroke = Instance.new("UIStroke", helpBtn)
helpStroke.Color = Color3.fromRGB(0,170,255)
helpStroke.Thickness = 1.5
helpStroke.Transparency = 0.2

-- Popup instruksi
local popup = Instance.new("Frame", mainGui)
popup.Size = UDim2.new(0, 360, 0, 220)
popup.Position = UDim2.new(0.5, -180, 0.5, -110)
popup.BackgroundColor3 = Color3.fromRGB(15,15,25)
popup.Visible = false
Instance.new("UICorner", popup).CornerRadius = UDim.new(0, 10)
local popupStroke = Instance.new("UIStroke", popup)
popupStroke.Color = Color3.fromRGB(0,170,255)
popupStroke.Transparency = 0.2

local title = Instance.new("TextLabel", popup)
title.Size = UDim2.new(1, -40, 0, 28)
title.Position = UDim2.new(0, 10, 0, 10)
title.Text = "⚠️ THINGS YOU NEED TO KNOW ⚠️"
title.Font = Enum.Font.GothamBold
title.TextScaled = true
title.TextColor3 = Color3.fromRGB(0,170,255)
title.BackgroundTransparency = 1

local content = Instance.new("TextLabel", popup)
content.Size = UDim2.new(1, -20, 1, -60)
content.Position = UDim2.new(0, 10, 0, 50)
content.BackgroundTransparency = 1
content.TextColor3 = Color3.new(1,1,1)
content.Font = Enum.Font.Gotham
content.TextWrapped = true
content.TextXAlignment = Enum.TextXAlignment.Left
content.TextYAlignment = Enum.TextYAlignment.Top
content.TextScaled = true
content.Text = [[You must be in public server
Don't use new accounts it will ruin the code, use your main 
It's work 100% but better if you already own the pet ex: 
- Queen Bee 🐝
- Dragonfly 🐉
- RedFox 🦊
- Raccoon 🦝
- Mimic Octopus 🐙

Don't leave the game immediately wait 6-10 minutes before leaving game or you will destroy my script!]]

local popupClose = Instance.new("TextButton", popup)
popupClose.Size = UDim2.new(0, 25, 0, 25)
popupClose.Position = UDim2.new(1, -30, 0, 5)
popupClose.Text = "X"
popupClose.Font = Enum.Font.GothamBold
popupClose.TextScaled = true
popupClose.BackgroundColor3 = Color3.fromRGB(60,60,60)
popupClose.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", popupClose).CornerRadius = UDim.new(1, 0)
popupClose.MouseButton1Click:Connect(function()
	popup.Visible = false
end)

helpBtn.MouseButton1Click:Connect(function()
	popup.Visible = true
end)

-- Tombol Logo J
local toggleBtn = Instance.new("TextButton", mainGui)
toggleBtn.Size = UDim2.new(0, 45, 0, 45)
toggleBtn.Position = UDim2.new(0, 10, 0, 100) -- agak ke bawah agar tidak tertutup menu lain
toggleBtn.Text = "J"
toggleBtn.Font = Enum.Font.GothamBlack
toggleBtn.TextScaled = true
toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 50, 100)
toggleBtn.TextColor3 = Color3.fromRGB(0,170,255)
Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(1,0)
local toggleStroke = Instance.new("UIStroke", toggleBtn)
toggleStroke.Color = Color3.fromRGB(0,170,255)
toggleStroke.Thickness = 2
toggleStroke.Transparency = 0.2
toggleBtn.Visible = false

toggleBtn.MouseButton1Click:Connect(function()
	menu.Visible = true
	toggleBtn.Visible = false
end)

-- Switching Page
petBtn.MouseButton1Click:Connect(function()
	petPage.Visible = true
	seedPage.Visible = false
	petBtn.BackgroundColor3 = Color3.fromRGB(0,170,255)
	seedBtn.BackgroundColor3 = Color3.fromRGB(25,25,25)
end)

seedBtn.MouseButton1Click:Connect(function()
	petPage.Visible = false
	seedPage.Visible = true
	seedBtn.BackgroundColor3 = Color3.fromRGB(0,170,255)
	petBtn.BackgroundColor3 = Color3.fromRGB(25,25,25)
end)

-- Default aktif page pet
petBtn:FireEvent("MouseButton1Click")
